# My Cooking App Example with React.js and Node.js

Greetings!

# How to run backend

In ther server folder run 1) `npm install` 2) `node server.js`

The rest of the document is in the client/README.md
